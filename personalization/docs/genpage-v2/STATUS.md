# GenPage v2 진행 상황

계획은 [PLAN.md](PLAN.md), 형식은 [DESIGN.md](DESIGN.md). 마일스톤 `GenPage v2`(#15).

예상은 착수 전에 적은 값을 고치지 않는다. 실제는 이슈 생성 · 브랜치 커밋 · 머지 시각에서 계산한다(추정으로 채우지 않는다).

## 묶음

| 묶음 | 이슈 | 상태 | 예상(착수 전) | 실제(이슈 생성 → 머지 · 결과, 벽시계) |
|---|---|---|---|---|
| A1 토큰 · 데이터 | #296 | 완료(#307) | 구현 1~2시간 · 생성 30~60분 | 1.9시간 |
| A2 콘텐츠 임베딩 | #297 | 완료(#306) | 1~2시간 | 0.4시간 |
| A3 모델 · 사전학습 | #298 | 완료(#308) | 구현 2~3시간 | 1.9시간 |
| A4 디코딩 · 평가 | #299 | 완료(#309) | 구현 2~3시간 | 2.6시간 |
| B 학습 · 절제 | #300 | 완료 — 선 못 넘음(#319 · #320) | 학습 2~6시간 × 여러 번 | 25.5시간 |
| C1 WBC | #301 | 실패 기록(#321), 뒤로 미룸 | 2~4시간 | — |
| C2 RL | #302 | 뒤로 미룸 | 4~8시간 | — |
| C3 증분 학습 | #303 | 뒤로 미룸(닫음) | 2~3시간 | — |
| C4 서빙 · 앱 | #304 | 완료(#322) | 3~5시간 | 16.4시간 |
| D 검증 · 문서 | #305 | 이 PR | 3~4시간 | — |
| 원문 대조 수정 | #310 | 완료(#319) | 구현 1~1.5시간 · 재생성 12분 | 13.6시간 |
| X1 문맥 일치 · X4 지연 예산 | #315 · #318 | 완료(#322) | 각 2~3시간 | 12.7시간 |
| X2 과부하 | #316 | 완료(#323) | 2~3시간 | 12.8시간 |
| X3 생성 중 품절 | #317 | 완료(#324) | 2~3시간 | 12.9시간 |
| X5 저장소 → 모델 입력 | #328 | 결과 나옴(이 PR) | 구현 반나절 · 측정 1~2시간 | 0.7시간(이슈 생성 → 결과) |

실제는 이슈 생성 시각부터 머지(또는 결과) 시각까지의 벽시계 시간이다. 여러 묶음을 동시에 진행했고 사람 · 코더가 그 시간 내내 한 묶음에 붙어 있지 않았으므로 작업량이 아니라 **달력 시간**으로 읽는다. 예상은 착수 전에 적은 값을 고치지 않았다.

## 기록

- 2026-09-25 03:25 KST 이슈 #296 ~ #305 생성, 계획 · 설계 문서 작성
- 2026-09-25 가상환경에 `transformers` 5.17.0 설치, `intfloat/multilingual-e5-small` 오프라인 로드 확인(384차원)
- 2026-09-25 설계 정정 두 가지(구현 전에 데이터를 보고 찾았다)
  - `article_id` 는 10자리 문자열이고 섹션은 56개가 아니라 57개다
  - 평가 대상은 "이력에 어휘 상품이 있는 고객"이 아니라 **그 주에 산 고객 전부**다. v1 의 `map_at_k` 는 정답이 있는 모든 고객(final 68,984명)을 분모로 두고 추천이 없으면 0으로 센다. 좁히면 선 0.023354 와 다른 집합을 채점하게 된다
- 2026-09-25 A1 · A2 · A3 · A4 를 동시에 착수. A3 · A4 는 A1 형식에만 기대므로 DESIGN 의 인터페이스로 가짜 데이터 테스트를 먼저 쓰고, A1 이 들어오면 실제 데이터로 맞춘다
- 교차 확인값: v1 final 어휘 82,759개(A1 final 어휘가 같아야 한다), v1 모델은 검증 주를 포함해 학습해 v1 기준선은 final 에서만 비교한다

## 결과

### 최종 — 홀드아웃 한 번 (2026-09-26 04:58)

고른 설정(base · 전체 문맥 · 1에폭 · 다시 사기 행 고정)으로 final 데이터에서 다시 학습(4,500단계)해 홀드아웃 주 68,984명 전체를 **한 번** 평가했다(5조각 병렬, 원자료 `holdout-final.json`).

| 홀드아웃 | MAP@12 | 이력에 어휘 상품이 있는 고객만 | 페이지 recall | 행 recall | 다양성 | 위반 |
|---|---|---|---|---|---|---|
| **v2** | **0.019268** | 0.020351 | **0.0691** | 0.5265 | 0.1560 | 0 |
| v1 모델 | 0.020930 | 0.022770 | 0.0401 | — | — | — |
| 마지막 구매 재추천 | 0.023446(하네스) · **0.023354**(v1 기록, 선) | 0.025508 | 0.0405 | — | — | — |
| 지난주 인기 | 0.008748 | 0.008825 | 0.0255 | — | — | — |

**판정: 선을 넘지 못했다**(선의 83%, v1 보다도 낮다). 규칙대로 다시 고르지 않는다. 결정은 [ADR-068](../../../docs/adr/ADR-068-genpage-v2-not-default.md).

### 선 넘기기 순서 5 — 2에폭 (2026-09-26 00:55)

`b-base-full` 과 같은 설정으로 2에폭(9,388단계, 5시간 43분, 마지막 학습 손실 4.527 — 1에폭 4.614). 같은 1만 명 표본 · 다시 사기 행 고정:

| 설정 | MAP@12 | 선(표본 0.02346) 대비 |
|---|---|---|
| base 1에폭 + 고정 | 0.01865 | 79% |
| base 2에폭 + 고정 | 0.01959 | 83% |

차이 0.00094 가 PLAN 규칙(0.001) 안이라 **짧은 쪽(1에폭)을 고른다.** 검증 주에서 둘 다 선 아래다. 규칙대로 고른 설정으로 final 데이터 재학습 → 홀드아웃을 한 번 본다(결과가 선 아래여도 다시 고르지 않는다).

처음 실행은 평가 단계에서 멈췄다. 밤사이 스크립트가 `${lim:+--limit $lim}` 를 한 인자로 넘겼다(zsh 는 변수를 공백으로 나누지 않는다). `${=lim:+...}` 로 고쳐 학습을 건너뛰고 평가부터 이었다.

### 선 넘기기 순서 4 — WBC 실패 (2026-09-25 14:07)

base(전체 문맥)에서 시작해, 다시 사기 행을 고정한 base 가 만든 학습 기간 페이지 1만 개를 노출로, 실제 다음 7일 구매를 라벨로 WBC 를 79단계(1에폭 · 배치 128) 했다. 노출 생성 약 1시간 40분(메모리 압박으로 스왑), 학습 3분.

| 조합(같은 1만 명) | MAP@12 | 페이지 recall | 위반 |
|---|---|---|---|
| base + 고정 | 0.01865 | 0.0625 | 0 |
| base + 고정 + WBC | **0.00276** | 0.0082 | 0 |

- 양성 비율이 0.6% 라 WBC 는 노출된 토큰의 로짓을 대부분 아래로 내렸다(WBC 손실 1.51 → 0.29)
- **노출되지 않은 토큰은 사전학습 로짓(소프트맥스 척도, 크다)을 그대로 가진다.** 탐욕 생성이 로짓을 값으로 읽으니, 한 번도 노출되지 않은 상품이 이긴다. 착수 전에 이 위험을 적었고 그대로 일어났다
- 원문은 노출 로그가 방대해 거의 모든 토큰이 노출된다. 여기서는 1만 페이지 · 79단계라 노출 범위가 좁다. 고치려면 노출을 크게 늘리거나(표본 · 탐색) 사전학습 손실을 같이 걸어 척도를 묶어야 한다 — 뒤로 미룬 ML(#301)에서 다룬다

다음은 PLAN 순서 5(최고 설정 2에폭). 백엔드 실험을 오늘 저녁에 끝내려고 순서 5 와 최종 확인은 밤사이에 돌린다(아래 절).

### 선 넘기기 — base (2026-09-25 13:45, 검증 주 1만 명 표본)

base(전체 문맥) 단독 MAP@12 0.01017 로 small 전체(0.00463)보다 0.0055 높아 PLAN 규칙(0.001)대로 **base 를 고른다**. 순서 2 · 3 을 base 로 다시 했다(5조각 병렬).

| 조합 | MAP@12 | 선(표본 0.02346) 대비 | 페이지 recall | 규칙 위반 |
|---|---|---|---|---|
| base 단독 | 0.01017 | 43% | 0.0457 | 0 |
| base + 다시 사기 행 고정 | 0.01865 | 79% | 0.0625 | 0 |
| base + 고정 + 후보(200 · 20, 고객당 평균 358개) | 0.01870 | 80% | 0.0683 | 0 |
| (참고) 마지막 구매 재추천 | 0.02346 | 100% | 0.0405 | — |

- 후보 집합은 아래 행을 바꿔 recall 을 올렸지만 첫 12개(대부분 다시 사기 행)는 그대로라 MAP@12 가 같았다
- **후보 (500 · 50) 은 돌리지 않았다.** (200 · 20) 이 MAP@12 를 바꾸지 않아 더 넓힌다고 첫 12개가 바뀔 이유가 없고, 같은 시간에 돌던 WBC 노출 생성이 CPU 를 못 받아 1시간 반째였다. PLAN 에 적은 두 점 중 하나를 건너뛴 것이다
- 선을 넘는 데 남은 것은 다시 사기 행 **안의 순서**다. 순서 4(WBC)가 실제 다음 주 구매로 그 순서를 배운다

### 원문 발견 1 판정 · 선 넘기기 진행 (2026-09-25 12:00)

**원문 발견 1 — 재현되지 않았다(반대).** 판정은 PLAN 규칙대로 검증 페이지 손실(eval.npz 앞 5,000개, 4,000단계 시점, 두 실행의 마지막 공통 평가 지점)로 한다.

| 비교 | 손실 | 변화 |
|---|---|---|
| 문맥 누적: small 상품만 → small 전체 | 4.8859 → 4.8991 | +0.27%(나빠짐) |
| 크기: small 전체 → base 전체 | 4.8991 → 4.5327 | −7.48% |

원문은 문맥 누적이 약 6.9%, 크기(7.5배)가 약 1.3% 였다. 여기서는 크기(2.2배)가 훨씬 컸고 문맥은 효과가 없었다. H&M 구매 데이터에서 나이 · 채널 · 가격 · 요일이 다음 주 구매를 가르는 신호가 약하다는 뜻으로 읽힌다(추정, 따로 확인하지 않았다). large 는 뒤로 미뤘다(PLAN).

**선 넘기기 순서 2 — 다시 사기 행 첫 행 고정**(b-small-full, 같은 1만 명): MAP@12 0.00463 → **0.01472**(3.2배), 페이지 recall 0.0299 → 0.0512(재구매 규칙 0.0405 보다 높다), 위반 0. 선(표본 0.02346)의 63%. 재구매 규칙은 최근 산 순서로 12개를 앞에 두고, v2 는 다시 사기 행 8개를 모델 순서로 두고 나머지 4개를 다른 행에서 채운다.

**평가 속도 문제.** 1만 명 평가가 CPU · MPS 모두 약 30분이다(생성이 파이썬 한 코어에 묶인다). 평가를 조각으로 나눠 여러 프로세스로 돌리는 옵션을 더한다. 같은 이유로 순서 4(WBC)의 노출을 5만 → **1만 개**로 줄였다(결과를 보기 전).

### B 결과 — small 6개(검증 주 1만 명 표본, 2026-09-25 10:25)

| 문맥 수준 | MAP@12 | 페이지 recall | 행 recall | 다양성 | 검증 페이지 손실(학습 끝, 5,000개) |
|---|---|---|---|---|---|
| 상품만 | 0.00521 | 0.0309 | 0.5304 | 0.1515 | 4.8859 |
| + 채널 | 0.00426 | 0.0299 | 0.5306 | 0.1533 | 4.8928 |
| + 시각 | 0.00442 | 0.0294 | 0.5262 | 0.1540 | 4.8961 |
| + 가격 | 0.00481 | 0.0300 | 0.5329 | 0.1546 | 4.8988 |
| + 프로필 | 0.00481 | 0.0311 | 0.5338 | 0.1530 | 4.8987 |
| 전체 | 0.00463 | 0.0299 | 0.5335 | 0.1536 | 4.8991 |
| (같은 표본) 마지막 구매 재추천 | 0.02346 | | | | |
| (같은 표본) 지난주 인기 | 0.00599 | | | | |

- 규칙 위반은 여섯 모두 0
- 원문 발견 1(문맥 쪽): 이 데이터 · 이 예산에서 문맥을 더해도 손실이 줄지 않았다(4.8859 → 4.8991, +0.27%). 크기 쪽(base)을 본 뒤 판정한다
- 여섯의 MAP@12 차이는 모두 0.001 안이라 PLAN 규칙대로 같은 것으로 본다. 동점일 때 고르는 규칙은 미리 적지 않았으므로 여기 적는다: **전체 문맥**을 고른다. 백엔드 실험 X1(앱이 주는 정보가 모자랄 때 입력이 얼마나 달라지나)은 모든 필드를 쓰는 모델이어야 의미가 있기 때문이다. 점수로 고른 게 아니라는 점을 분명히 해 둔다
- 모델 단독은 선의 약 1/5 이다. 첫 12개가 대부분 새 상품 섹션 행으로 채워지고, 선을 만드는 재구매 자리를 모델이 앞에 두지 않는다

### B 예산 확정 (측정 전, 2026-09-25)

150단계 속도 측정(validate · 배치 128 · MPS, 50~150단계 평균):

| 실행 | 크기 · 문맥 | 파라미터 | 단계당 |
|---|---|---|---|
| (a) | small · 이력만 | 21,557,632 | 0.224초 |
| (b) | small · 전체 | 21,557,632 | 0.278초 |
| (c) | large · 이력만 | 77,678,976 | 1.157초 |
| (d) | base · 전체 | 47,060,736 | 1.106초 |

규칙(PLAN: 가장 큰 실행이 3시간 안) → (c) 기준 9,334단계. **9,000단계로 고정한다.** 네 실행 모두: 배치 128(= 115만 예시, 약 1.9에폭), 학습률 3e-4, 워밍업 500, 코사인 감소, 대체 토큰 5%, 시드 7, 1,000단계마다 eval.npz 앞 5,000개 페이지 손실. 순서 (a) → (b) → (d) → (c), 한 번에 하나(MPS 를 나눠 쓰면 속도가 흔들린다).

예상: (a) 34분 · (b) 42분 · (c) 2.9시간 · (d) 2.8시간, 합 약 7시간.

### B 예산 변경 (비교 결과를 보기 전, 2026-09-25 05:55)

**9,000단계 → 4,500단계(1에폭)로 줄인다.** 네 실행 모두 같다. final 재학습도 4,500단계다.

- 첫 실행 (a) 가 4,694단계에서 멈췄다. `--epochs` 기본값이 1이라 1에폭이 끝나면 `--max-steps 9000` 과 상관없이 끝나고, 코사인 스케줄은 9,000단계 기준이라 학습률이 절반쯤에서 잘렸다. 약속한 조건이 아니라서 (a) 와 진행 중이던 (b) 를 버렸다(`ckpt/_invalid/`)
- 다시 시작하는 김에 예산을 줄였다. 9,000단계면 비교 결과까지 약 7시간이고 그중 5.7시간이 (c) · (d) 다. 스모크의 검증 손실이 500 · 1,000 · 1,500 · 2,000단계에서 6.15 · 5.57 · 5.35 · 5.32 로 이미 평평해지고 있었다
- 모델 크기 대비(large 는 small 의 3.6배)를 지키려고 크기를 줄이지 않고 단계를 줄였다
- 대가: 모든 실행이 덜 배운다. 절대 점수가 낮게 나올 수 있고, 선을 근소하게 못 넘으면 "더 긴 학습"이 남은 변수라고 적는다
- 이 변경은 네 실행의 비교 결과(검증 MAP@12 · 손실)를 하나도 보기 전에 했다. 본 것은 (a) 의 학습 손실 로그뿐이다

실행: `--epochs 1 --max-steps 4500`, 나머지(배치 128 · 학습률 3e-4 · 워밍업 500 · 시드 7)는 같다. 예상: (a) 13분 · (b) 19분 · (d) 83분 · (c) 87분, 합 약 3.4시간.

### A1 데이터셋 · A4 기준선 — final (2026-09-25)

v2 하네스(`genpage2.dataset` + `genpage2.evaluate`)가 v1 과 같은 잣대인지 먼저 확인했다. 이게 맞아야 v2 점수를 v1 · 선과 비교할 수 있다.

| 항목 | v2 | v1 기록 | 판정 |
|---|---|---|---|
| 상품 어휘(2020-09-16 이전 10회 이상) | 82,759 | 82,759 | 같음 |
| 평가 고객(홀드아웃 주 구매 고객 전부) | 68,984 | 68,984 | 같음 |
| v1 모델 MAP@12 | 0.020930 | 0.020930 | 같음 |
| 지난주 인기 MAP@12 | 0.008748 | 0.008748(popular_recent7d) | 같음 |
| 마지막 구매 재추천 MAP@12 | 0.023446 | 0.023354 | +0.000092, 허용 ±0.0002 안 |

재구매 차이는 validate 에서 −0.000085, final 에서 +0.000092 로 방향은 다르고 크기는 비슷하다. 같은 날 산 상품의 순서 처리 차이로 보이지만 확인하지 않았다. 판정 선은 v1 기록값 **0.023354 를 그대로 쓴다**(v2 하네스 값 0.023446 이 더 높으므로 선을 넘었다고 말하려면 둘 다 넘어야 한다).

생성: validate 274초 · 최대 메모리 9.2GB, final 417초 · 9.2GB(`/usr/bin/time -l`). 학습 예시 validate 600,779 · final 595,707(상한 200만 미만). 정답 페이지 행 수 중앙값 2 · p90 4, 상품 수 중앙값 2 · p90 7.

기준선 평가: final 148초(v1 모델 추천 포함), validate 8초.

### 스모크 학습 (small · 전체 문맥 · 2,000단계 · 배치 128 · MPS)

비교에 쓰지 않는다. 경로가 끝까지 도는지와 속도만 본다.

| 단계 | 학습 손실 | 검증 페이지 손실(5,000개) |
|---|---|---|
| 50 | 11.06 | |
| 500 | 6.07 | 6.15 |
| 1,000 | 5.49 | 5.57 |
| 1,500 | 5.34 | 5.35 |
| 2,000 | 5.35 | 5.32 |

556초, 단계당 0.28초. 1에폭(4,694단계) 약 22분. 파라미터 21,557,632. 첫 행 정확도는 0.2156 에서 움직이지 않았다(가장 흔한 행 하나만 고르는 단계).

### A1 데이터셋 — validate (2026-09-25)

| 항목 | v2 | v1 · 기준 | 판정 |
|---|---|---|---|
| 상품 어휘(2020-09-09 이전 10회 이상) | 82,024 | v1 validate 82,024 | 같음 |
| 평가 고객(검증 주 구매 고객 전부) | 72,019 | — | |
| 이력에 어휘 상품이 있는 평가 고객 | 92.5% | | |
| 정답 페이지가 빈 평가 고객(그 주 구매가 전부 어휘 밖) | 2.8% | | |
| 학습 예시 | 600,779 (상한 200만 미만) | | |
| 마지막 구매 재추천 MAP@12(검증 주) | 0.023498 | ADR-054 검증 0.023583 | 차이 0.000085, 허용 ±0.0002 안 |
| 지난주 인기 MAP@12(검증 주) | 0.006604 | | |
| 생성 시간 | 4분 30초(파일 쓰기까지) | 계획 30~60분 | |

재구매 차이는 같은 날 산 상품의 순서 처리로 보인다(v1 은 `sort_values` 기본 정렬, v2 는 원본 순서 유지). 확인은 하지 않았다.

**실측에서 드러난 결함 두 개** (단위 테스트는 통과했다)
- 통계 계산이 58분째 끝나지 않았다. `np.int32 in range` 는 파이썬 int 가 아니라 빠른 경로를 못 타고 원소를 하나씩 비교한다(1회 3.7ms, int 는 0.4µs). 200만 예시에서 수십 시간이 된다
- 학습 첫 단계에서 죽었다. 학습 루프가 넘긴 상품 구간 끝(`item_end`)을 손실 함수가 대체 토큰 함수에 넘기지 않았다. 테스트는 다른 인자 모양으로 불러 이 경로를 안 탔다. 학습 루프 전체를 타는 테스트를 더하게 했다

### A2 콘텐츠 임베딩 (2026-09-25)

| 항목 | 값 |
|---|---|
| 상품 수 · 차원 | 105,542 · 384 (float16, 81MB) |
| 장치 · 시간 | MPS · 263초(배치 128). 표본 3,000개 CPU 27초로 선형 추산한 17분보다 짧았다 |
| 같은 섹션 쌍 코사인 평균 | 0.863 (다른 섹션 0.817) |
| 같은 상품 종류 쌍 | 0.897 (다른 종류 0.805) |
| 결측 조각 | 실제 텍스트에 `<NA>` · `nan` 0건(검토 지적으로 확인) |

명령: `cd personalization && $PY -m genpage2.content --device mps --batch 128 && $PY -m genpage2.content --check`
