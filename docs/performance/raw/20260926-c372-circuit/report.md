# #372 서킷 분리 전후 (2026-09-26)

- 도구: `CircuitSplitExperimentTest`(같은 파일을 바꾸기 전 origin/main 과 바꾼 브랜치에 넣어 실행)
- 부하: PG 스텁, 조회 초당 20건 · 승인 초당 10건, 조건마다 60초, 동시 호출 상한 없음(서킷만 본다)
- 조건: Q10 · Q30 · Q100(조회 실패율, 승인 정상), A100(승인 전면 장애, 조회 정상)
- 같은 맥의 다른 작업: 측정 시작 때 GenPage 모델 서버 없음(`ps-before.txt` 비어 있음). 건수 측정이라 CPU 경합에 흔들리지 않는다
- `smoke-5s.txt`: 본 측정 전 5초 시험 실행. 여기서 A100 전의 예측이 틀린 것을 보고 이슈에 기준 2 변경을 먼저 적었다
- `circuit-before.json` · `circuit-after.json`: 본 측정

유령 미확정 = PG 스텁이 받은 적 없는 주문인데 결과가 TIMEOUT(미확정)인 승인.
