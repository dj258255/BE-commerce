# 기여 규칙

이 저장소는 결제 도메인의 정합성을 다룬다. 변경은 **동작보다 근거를 먼저** 남긴다.

## 작업 단위

작업은 이슈 하나 = 브랜치 하나 = PR 하나로 자른다. 이슈 번호와 PR 번호는 같은 시퀀스를 공유한다.

| 단계 | 규칙 |
| --- | --- |
| 이슈 | 배경 / 할 일 / 검증 기준을 적는다. 마일스톤과 라벨을 붙인다 |
| 브랜치 | `feat/…`, `fix/…`, `docs/…`, `chore/…`, `refactor/…` |
| PR | 본문에 `Closes #NN`으로 이슈를 닫는다. CI가 초록이어야 머지한다 |
| 머지 | **머지 커밋**으로 머지한 뒤 **브랜치를 지운다**(원격·로컬) |

**머지한 브랜치는 지운다.** 남겨 두면 쌓여서 무엇이 아직 살아 있는지가 안 보인다 — 원격 브랜치가
79개까지 늘었던 적이 있다(그중 71개가 머지된 PR의 브랜치였다). 지워도 되돌릴 수 있다: 머지된 PR에서
"Restore branch"로 되살아난다. 남기는 예외는 **되돌릴 지점**(`backup/…`·`master-snapshot`),
**다른 열린 이슈가 "이 브랜치를 쓴다"고 명시한 것**, **아직 머지되지 않은 작업** 셋뿐이고 이유를 적는다.

### 머지 커밋으로 한다 (2026-09-23부터)

스쿼시로 합치면 **브랜치의 커밋 메시지가 사라진다.** 이 저장소는 커밋 하나하나에 "왜"를 적어 왔는데,
스쿼시는 그걸 PR 제목 한 줄로 눌러 버린다. 그래서 머지 커밋을 쓴다 — 개별 메시지가 `main`에 남는다.

**저장소 설정에서 스쿼시·리베이스를 껐다.** 규칙만 적어 두면 또 갈리기 때문이다(실제로 갈렸다).
머지 커밋 제목은 PR 제목을 그대로 쓰도록 맞춰 뒀으므로 `gh pr merge --merge` 만 하면 된다.

```bash
gh pr merge <번호> --merge --delete-branch
```

⚠ **머지 여부를 `git branch --merged`로 판단하지 않는다.** 2026-09-23 이전에 들어간 PR 63건은
**스쿼시라 브랜치 커밋이 `main`의 조상이 아니다** — 머지된 브랜치도 "안 머지"로 나온다(거짓 음성).
그 시기 브랜치의 판단은 **PR 상태**로 한다:
`gh pr list --state all --json number,state,headRefName`

내용으로 확인하려면 브랜치가 건드린 파일만 `main`과 대조한다. 단 **`main`이 그 뒤로 같은 파일을
더 고쳤으면 차이가 나므로**, 차이가 있다고 해서 안 머지된 것은 아니다.

## 커밋

- 한국어로 쓴다. 제목은 **무엇을 했는지** 한 줄, 본문은 불릿으로 **왜**를 적는다.
- 이모지와 도구·에이전트 언급은 쓰지 않는다.
- 설계 결정은 커밋 로그에 흩어 두지 않는다. 트레이드오프가 있는 결정은 ADR 한 편으로 남기고 커밋에서 참조한다.

```text
정산 배치가 하루 500건에 묶여 있던 것을 푼다

- 처리 상한을 설정으로 꺼내 배치 크기와 분리
- 상한 도달 시 다음 틱으로 이월, 유실 없음을 테스트로 고정
```

## 검증

응답 코드만 보고 끝내지 않는다. 상태가 실제로 확정됐는지 DB를 재조회한다.

```bash
export JAVA_HOME=/opt/homebrew/Cellar/openjdk@21/21.0.9/libexec/openjdk.jdk/Contents/Home
./gradlew -p commerce clean test --console=plain          # 기본 스위트(chaos/integration 태그 제외)
./gradlew -p commerce/consumer-app build               # 독립 프로젝트
```

- 문서와 코드가 갈라지면 테스트가 먼저 막는다 — `ErdDocMatchesSchemaTest`(스키마↔ERD),
  `ApiSpecErrorCodesTest`(에러 코드↔API 스펙), `ModularityTests`(모듈 경계). 셋을 함께 고친다.
- **건너뛴 테스트는 통과가 아니다.** 환경변수·키·아티팩트가 없으면 테스트는 조용히 skip되고
  빌드는 성공한다 — 그러면 검증하지 않은 기능이 초록불 아래로 나간다. CI가 JUnit XML의 skip을
  읽어, 실행하기로 한 테스트가 실제로 돌았는지까지 확인한다.

  ```bash
  ./gradlew -p commerce test
  python3 tools/check_skipped_tests.py commerce/build/test-results/test ci/allowed-skips.txt
  ```

  의도적으로 건너뛰는 테스트는 `ci/allowed-skips.txt`에 **이유와 함께** 적는다.
  이유 없는 예외는 다음 사람이 못 지운다.
- **깨진 링크는 아무도 안 잡는다.** 디렉터리를 옮기면 문서 링크는 조용히 404가 된다 —
  빌드도 테스트도 안 본다. 그래서 검사로 고정한다.

  ```bash
  python3 tools/check_doc_links.py
  ```
- 실 인프라가 필요한 검증은 별도 태스크로 분리한다(`integrationTest`, `chaosTest`, `bench`).
- 스키마를 바꾸면 Flyway 마이그레이션을 추가하고 `ddl-auto=validate`로 실기동을 확인한다.

## 문서

- 구현된 사실, 측정 결과, 아직 하지 않은 계획을 구분해 적는다.
- 결정이 바뀌면 이전 기록을 지우지 않고 상단 상태와 후속 ADR로 연결한다.
- 성능 수치에는 환경·부하 모델·표본 크기를 함께 적는다.
